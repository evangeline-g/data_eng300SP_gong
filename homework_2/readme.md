## Running the Code

To run the source code, go to `Homework_2.ipynb` and run all cells. Make sure to have all the required packages installed.

## Expected Output

The expected outputs are the recommendations for:
- Cold user in the pre-1980 dataset,
- Top user in the pre-1980 dataset,
- Cold user in the full dataset,
- Top user in the full dataset,
- Personal profile in the full dataset.

In addition to being printed in the notebook, those outputs are also available in the S3 `nky8459-de300-hw2` bucket, under the `/output` folder. They're stored as `json` format.

For this assignment, there will not be a `.pdf` or `.html` report because all choices are explained in the source code notebook.